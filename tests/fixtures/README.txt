All fixtures are synthetic and generated below a per-test temporary directory.
No productive paths, mounts, containers, images, networks, units, cron jobs,
Filen data, backup media or secret files are accessed. Dummy secret values must
start with DUMMY_TEST_ONLY_.
